# Environment Configuration for Malware Detection Gateway Frontend

# API Base URL - Point to your backend server
# For production, use the server's IP or domain (e.g., http://192.168.1.100:8000)
# Use "auto" to automatically detect the current host (works for both localhost and VM)
VITE_API_URL=auto

# API Port - Default port for the backend API
VITE_API_PORT=8000

# Optional: Enable debug mode
# VITE_DEBUG=true