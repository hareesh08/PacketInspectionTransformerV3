# Environment Configuration for Malware Detection Gateway Frontend

# API Base URL - Point to your backend server
# Use "auto" to automatically detect the correct URL based on environment:
# - localhost: Uses http://localhost:8000 (for local development)
# - production: Uses /api (for nginx proxy)
VITE_API_URL=auto

# API Port - Only needed for localhost (development)
VITE_API_PORT=8000

# Optional: Enable debug mode
# VITE_DEBUG=true